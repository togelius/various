diff --git a/puzzlescript_cpp/src/bindings.cpp b/puzzlescript_cpp/src/bindings.cpp
index f2a09d0..2f42ca4 100644
--- a/puzzlescript_cpp/src/bindings.cpp
+++ b/puzzlescript_cpp/src/bindings.cpp
@@ -58,6 +58,10 @@ PYBIND11_MODULE(_puzzlescript_cpp, m) {
              "Whether the game is in a winning state")
         .def("is_againing", &Engine::isAgaining,
              "Whether the game wants an 'again' tick")
+        .def("get_rule_fire_counts", &Engine::getRuleFireCounts,
+             "scriptprof: dict of rule source line number -> number of times applied")
+        .def("reset_rule_fire_counts", &Engine::resetRuleFireCounts,
+             "scriptprof: zero the rule-firing counters")
         .def("get_objects", [](const Engine& e) {
             const auto& objs = e.getObjects();
             return py::array_t<int32_t>(
diff --git a/puzzlescript_cpp/src/engine.cpp b/puzzlescript_cpp/src/engine.cpp
index 2e14c32..5279dda 100644
--- a/puzzlescript_cpp/src/engine.cpp
+++ b/puzzlescript_cpp/src/engine.cpp
@@ -85,6 +85,7 @@ Engine::Engine(Engine&& other) noexcept
       winning_(other.winning_),
       againing_(other.againing_),
       rng_(other.rng_),
+      ruleFireCounts_(std::move(other.ruleFireCounts_)),
       curLevel_(other.curLevel_),
       _o1(std::move(other._o1)), _o2(std::move(other._o2)),
       _o2_5(std::move(other._o2_5)), _o3(std::move(other._o3)),
@@ -134,6 +135,7 @@ Engine& Engine::operator=(Engine&& other) noexcept {
         winning_ = other.winning_;
         againing_ = other.againing_;
         rng_ = other.rng_;
+        ruleFireCounts_ = std::move(other.ruleFireCounts_);
         curLevel_ = other.curLevel_;
         _o1 = std::move(other._o1); _o2 = std::move(other._o2);
         _o2_5 = std::move(other._o2_5); _o3 = std::move(other._o3);
@@ -452,6 +454,15 @@ void Engine::loadLevel(int levelIndex) {
         processInput(-1);
         againing_ = false;
     }
+
+    // scriptprof: settle the win flag against the board we just loaded, the
+    // same way restoreLevel does. Some levels satisfy their win conditions
+    // before any move -- a `no X` condition where X only ever appears once the
+    // player acts, for instance -- and leaving the flag hard-false here made
+    // loadLevel and restoreLevel disagree about the very same board. A search
+    // restores and so saw the win; a replay only loaded and so did not, which
+    // reported the solution as unreplayable (Flood and Hitori, every level).
+    winning_ = checkWin();
 }
 
 // ============================================================
@@ -468,6 +479,20 @@ LevelBackup Engine::backupLevel() const {
 void Engine::restoreLevel(const LevelBackup& bak) {
     level_.objects = bak.dat;
     level_.calculateRowColMasks();
+    // scriptprof: a backup holds the object grid and nothing else, but the
+    // engine also carries per-turn state: the command queue, the `again` flag
+    // and the cached win flag. Leaving those in place lets one search branch
+    // contaminate the next. Concretely, when a solver skips a winning
+    // successor because it has seen that grid before, it skips the win check
+    // too, so `winning_` stays set and the next sibling action inherits it;
+    // the solver then reports a win with an action sequence that does not
+    // produce one (Angize returns [0,0] where the winning move is ACTION).
+    // A `win` command left in the queue leaks the same way, because checkWin
+    // consults the queue before it looks at the board.
+    level_.commandQueue.clear();
+    againing_ = false;
+    winning_ = false;
+    winning_ = checkWin();
 }
 
 void Engine::restart() {
@@ -1135,6 +1160,9 @@ bool Engine::ruleTryApply(Rule& rule) {
 
     if (!matches.empty()) {
         ruleQueueCommands(rule);
+        // scriptprof: a rule "fires" when it rewrites the level, or, for a
+        // command-only rule, whenever its pattern matches.
+        if (result || !rule.hasReplacements) ruleFireCounts_[rule.lineNumber]++;
     }
 
     return result;
@@ -1163,6 +1191,7 @@ bool Engine::applyRandomRuleGroup(std::vector<Rule*>& ruleGroup) {
     Rule& rule = *ruleGroup[match.ruleIndex];
     int delta = level_.delta_index(rule.direction);
     bool modified = ruleApplyAt(rule, match.tuple, false, delta);
+    if (modified || !rule.hasReplacements) ruleFireCounts_[rule.lineNumber]++;  // scriptprof
 
     ruleQueueCommands(rule);
     return modified;
diff --git a/puzzlescript_cpp/src/engine.h b/puzzlescript_cpp/src/engine.h
index c8e72e8..77a1014 100644
--- a/puzzlescript_cpp/src/engine.h
+++ b/puzzlescript_cpp/src/engine.h
@@ -117,6 +117,10 @@ public:
     bool isWinning() const { return winning_; }
     bool isAgaining() const { return againing_; }
 
+    // scriptprof: rule-firing instrumentation (keyed by rule source line number)
+    const std::map<int, long long>& getRuleFireCounts() const { return ruleFireCounts_; }
+    void resetRuleFireCounts() { ruleFireCounts_.clear(); }
+
     // Get idDict for interpreting object bits
     const std::vector<std::string>& getIdDict() const { return idDict_; }
 
@@ -164,6 +168,8 @@ private:
     bool winning_ = false;
     bool againing_ = false;
     RNG rng_;
+    // scriptprof: how many times each source rule (by line number) has applied
+    std::map<int, long long> ruleFireCounts_;
     int curLevel_ = 0;
 
     // Scratch BitVecs (to avoid repeated allocation in hot paths)
diff --git a/puzzlescript_cpp/src/solver.cpp b/puzzlescript_cpp/src/solver.cpp
index be08cc4..1b1aed5 100644
--- a/puzzlescript_cpp/src/solver.cpp
+++ b/puzzlescript_cpp/src/solver.cpp
@@ -66,6 +66,57 @@ double elapsedSeconds(const Clock::time_point& start) {
     return std::chrono::duration<double>(Clock::now() - start).count();
 }
 
+// scriptprof: adaptive clock polling for the search loops.
+//
+// Upstream consults the clock every 1000 expansions, which assumes expansions
+// are cheap. In games whose rules loop heavily a single expansion can take
+// tens of milliseconds, so a 1s budget could run for 78s. A small fixed
+// stride fixes that but taxes fast searches, which poll far more often than
+// they need to.
+//
+// This poller measures how long expansions actually take and retunes its
+// stride to land roughly kPollsPerBudget checks inside the budget. Slow games
+// converge to checking every iteration; fast games settle on a stride wider
+// than upstream's, so they poll less than before.
+constexpr int kPollsPerBudget = 64;
+constexpr long long kMaxStride = 4096;
+
+class TimeoutPoller {
+public:
+    TimeoutPoller(const Clock::time_point& start, int timeoutMs)
+        : timeoutMs_(timeoutMs), lastTime_(start) {}
+
+    // Call once per iteration with a monotonically increasing counter.
+    bool expired(const Clock::time_point& start, long long iter) {
+        if (timeoutMs_ <= 0 || iter < nextIter_) {
+            return false;
+        }
+        const auto now = Clock::now();
+        if (std::chrono::duration<double, std::milli>(now - start).count() > timeoutMs_) {
+            return true;
+        }
+        const double sinceMs = std::chrono::duration<double, std::milli>(now - lastTime_).count();
+        const long long done = iter - lastIter_;
+        if (done > 0 && sinceMs > 0.0) {
+            const double perIterMs = sinceMs / static_cast<double>(done);
+            const double targetMs = std::max(1.0, static_cast<double>(timeoutMs_) / kPollsPerBudget);
+            const long long tuned = static_cast<long long>(targetMs / perIterMs);
+            stride_ = std::min(kMaxStride, std::max(1LL, tuned));
+        }
+        lastTime_ = now;
+        lastIter_ = iter;
+        nextIter_ = iter + stride_;
+        return false;
+    }
+
+private:
+    int timeoutMs_;
+    Clock::time_point lastTime_;
+    long long lastIter_ = 0;
+    long long nextIter_ = 0;
+    long long stride_ = 16;
+};
+
 bool timedOut(const Clock::time_point& start, int timeoutMs) {
     if (timeoutMs <= 0) {
         return false;
@@ -86,8 +137,12 @@ std::vector<int> actionsForEngine(const Engine& engine) {
 }
 
 bool processInputSearch(Engine& engine, int action) {
+    // scriptprof: a rule that keeps issuing `again` never settles, and this
+    // loop runs inside a single expansion, where the solver's timeout is not
+    // consulted. Cap it at the same 1000 ticks as prof.engine.step, so a
+    // search and a replay of it stop at the same place.
     bool changed = engine.processInput(action);
-    while (engine.isAgaining()) {
+    for (int i = 0; i < 1000 && engine.isAgaining(); ++i) {
         changed = engine.processInput(-1) || changed;
     }
     return changed;
@@ -133,6 +188,21 @@ std::vector<int> reconstructSolution(
     return actions;
 }
 
+// scriptprof: the action list for a win reached from `parentDat` via `action`.
+// A winning successor is not always a new entry in `parents`: a rule can issue
+// the `win` command without moving anything, leaving the board identical to its
+// parent's. Reconstructing from the parent and appending the action covers both
+// cases.
+std::vector<int> solutionVia(
+    const StateVec& parentDat,
+    int action,
+    const std::unordered_map<StateVec, ParentInfo, StateVecHash>& parents
+) {
+    std::vector<int> actions = reconstructSolution(parentDat, parents);
+    actions.push_back(action);
+    return actions;
+}
+
 int selectMCTSChild(const std::vector<MCTSNode>& nodes, int nodeIndex, double explorationConstant) {
     const MCTSNode& node = nodes[nodeIndex];
     double bestScore = -std::numeric_limits<double>::infinity();
@@ -262,8 +332,9 @@ RandomRolloutResult randomRolloutRaw(Engine& engine, int maxIters, int timeoutMs
     std::mt19937 rng(std::random_device{}());
     std::uniform_int_distribution<int> actionDist(0, actionCount(engine) - 1);
 
+    TimeoutPoller poller(start, timeoutMs);
     for (int i = 0; i < maxIters; ++i) {
-        if (i % 1000 == 0 && timedOut(start, timeoutMs)) {
+        if (poller.expired(start, i)) {
             result.iterations = i;
             result.time = elapsedSeconds(start);
             result.timeout = true;
@@ -323,6 +394,12 @@ SolverResult solveRandom(Engine& engine, int maxLength, int maxIters, int timeou
 SolverResult solveBFS(Engine& engine, int maxIters, int timeoutMs) {
     const auto start = Clock::now();
     const LevelBackup initialState = engine.backupLevel();
+    // scriptprof: a level already satisfying its win conditions is won in zero
+    // moves. Reporting that honestly lets callers treat it as the degenerate
+    // level it is, instead of attributing the win to the first action tried.
+    if (engine.isWinning()) {
+        return makeSolverResult(engine, true, {}, 0, start, engine.getScore(), initialState, false);
+    }
     std::queue<LevelBackup> frontier;
     std::unordered_map<StateVec, ParentInfo, StateVecHash> parents;
     frontier.push(initialState);
@@ -332,9 +409,10 @@ SolverResult solveBFS(Engine& engine, int maxIters, int timeoutMs) {
     LevelBackup bestState = initialState;
     double bestScore = engine.getScore();
     int iterations = 0;
+    TimeoutPoller poller(start, timeoutMs);
 
     while (!frontier.empty() && iterations < maxIters) {
-        if (iterations % 1000 == 0 && timedOut(start, timeoutMs)) {
+        if (poller.expired(start, iterations)) {
             return makeSolverResult(engine, false, bestActions, iterations, start, bestScore, bestState, true);
         }
 
@@ -344,6 +422,28 @@ SolverResult solveBFS(Engine& engine, int maxIters, int timeoutMs) {
         for (int action : actionsForEngine(engine)) {
             engine.restoreLevel(parentState);
             const bool changed = processInputSearch(engine, action);
+
+            // scriptprof: check for a win before the guards below. A rule may
+            // win by issuing the `win` command without altering a single tile
+            // (Angize is won by ACTION alone), so the `!changed` guard would
+            // discard the winning action; and when the resulting board repeats
+            // one already seen, the `parents` guard would discard it too. Both
+            // paths previously left `winning_` set for the next sibling to
+            // inherit, which is how a solver came to report a win with an
+            // action sequence that does not produce one.
+            if (engine.isWinning()) {
+                return makeSolverResult(
+                    engine,
+                    true,
+                    solutionVia(parentState.dat, action, parents),
+                    iterations,
+                    start,
+                    engine.getScore(),
+                    engine.backupLevel(),
+                    false
+                );
+            }
+
             if (!changed) {
                 continue;
             }
@@ -379,6 +479,12 @@ SolverResult solveBFS(Engine& engine, int maxIters, int timeoutMs) {
 SolverResult solveAStar(Engine& engine, int maxIters, int timeoutMs) {
     const auto start = Clock::now();
     const LevelBackup initialState = engine.backupLevel();
+    // scriptprof: a level already satisfying its win conditions is won in zero
+    // moves. Reporting that honestly lets callers treat it as the degenerate
+    // level it is, instead of attributing the win to the first action tried.
+    if (engine.isWinning()) {
+        return makeSolverResult(engine, true, {}, 0, start, engine.getScore(), initialState, false);
+    }
     std::priority_queue<PriorityNode, std::vector<PriorityNode>, AStarComparator> frontier;
     std::unordered_map<StateVec, ParentInfo, StateVecHash> parents;
     parents.emplace(initialState.dat, ParentInfo{initialState.dat, -1});
@@ -387,10 +493,11 @@ SolverResult solveAStar(Engine& engine, int maxIters, int timeoutMs) {
     LevelBackup bestState = initialState;
     double bestScore = engine.getScore();
     int totalIters = 0;
+    TimeoutPoller poller(start, timeoutMs);
     std::mt19937 rng(std::random_device{}());
 
     while (!frontier.empty() && totalIters < maxIters) {
-        if (totalIters % 1000 == 0 && timedOut(start, timeoutMs)) {
+        if (poller.expired(start, totalIters)) {
             return makeSolverResult(
                 engine,
                 false,
@@ -411,6 +518,28 @@ SolverResult solveAStar(Engine& engine, int maxIters, int timeoutMs) {
         for (int action : actions) {
             engine.restoreLevel(current.state);
             const bool changed = processInputSearch(engine, action);
+
+            // scriptprof: check for a win before the guards below. A rule may
+            // win by issuing the `win` command without altering a single tile
+            // (Angize is won by ACTION alone), so the `!changed` guard would
+            // discard the winning action; and when the resulting board repeats
+            // one already seen, the `parents` guard would discard it too. Both
+            // paths previously left `winning_` set for the next sibling to
+            // inherit, which is how a solver came to report a win with an
+            // action sequence that does not produce one.
+            if (engine.isWinning()) {
+                return makeSolverResult(
+                    engine,
+                    true,
+                    solutionVia(current.state.dat, action, parents),
+                    totalIters,
+                    start,
+                    engine.getScore(),
+                    engine.backupLevel(),
+                    false
+                );
+            }
+
             if (!changed) {
                 continue;
             }
@@ -461,6 +590,12 @@ SolverResult solveAStar(Engine& engine, int maxIters, int timeoutMs) {
 SolverResult solveGBFS(Engine& engine, int maxIters, int timeoutMs) {
     const auto start = Clock::now();
     const LevelBackup initialState = engine.backupLevel();
+    // scriptprof: a level already satisfying its win conditions is won in zero
+    // moves. Reporting that honestly lets callers treat it as the degenerate
+    // level it is, instead of attributing the win to the first action tried.
+    if (engine.isWinning()) {
+        return makeSolverResult(engine, true, {}, 0, start, engine.getScore(), initialState, false);
+    }
     std::priority_queue<PriorityNode, std::vector<PriorityNode>, GBFSComparator> frontier;
     std::unordered_map<StateVec, ParentInfo, StateVecHash> parents;
     parents.emplace(initialState.dat, ParentInfo{initialState.dat, -1});
@@ -469,10 +604,11 @@ SolverResult solveGBFS(Engine& engine, int maxIters, int timeoutMs) {
     LevelBackup bestState = initialState;
     double bestScore = engine.getScore();
     int totalIters = 0;
+    TimeoutPoller poller(start, timeoutMs);
     std::mt19937 rng(std::random_device{}());
 
     while (!frontier.empty() && totalIters < maxIters) {
-        if (totalIters % 1000 == 0 && timedOut(start, timeoutMs)) {
+        if (poller.expired(start, totalIters)) {
             return makeSolverResult(
                 engine,
                 false,
@@ -493,6 +629,28 @@ SolverResult solveGBFS(Engine& engine, int maxIters, int timeoutMs) {
         for (int action : actions) {
             engine.restoreLevel(current.state);
             const bool changed = processInputSearch(engine, action);
+
+            // scriptprof: check for a win before the guards below. A rule may
+            // win by issuing the `win` command without altering a single tile
+            // (Angize is won by ACTION alone), so the `!changed` guard would
+            // discard the winning action; and when the resulting board repeats
+            // one already seen, the `parents` guard would discard it too. Both
+            // paths previously left `winning_` set for the next sibling to
+            // inherit, which is how a solver came to report a win with an
+            // action sequence that does not produce one.
+            if (engine.isWinning()) {
+                return makeSolverResult(
+                    engine,
+                    true,
+                    solutionVia(current.state.dat, action, parents),
+                    totalIters,
+                    start,
+                    engine.getScore(),
+                    engine.backupLevel(),
+                    false
+                );
+            }
+
             if (!changed) {
                 continue;
             }
@@ -543,6 +701,12 @@ SolverResult solveGBFS(Engine& engine, int maxIters, int timeoutMs) {
 SolverResult solveMCTS(Engine& engine, const MCTSOptions& options) {
     const auto start = Clock::now();
     const LevelBackup initialState = engine.backupLevel();
+    // scriptprof: a level already satisfying its win conditions is won in zero
+    // moves. Reporting that honestly lets callers treat it as the degenerate
+    // level it is, instead of attributing the win to the first action tried.
+    if (engine.isWinning()) {
+        return makeSolverResult(engine, true, {}, 0, start, engine.getScore(), initialState, false);
+    }
     LevelBackup bestState = initialState;
     double bestScore = engine.getScore();
 
diff --git a/puzzlescript_nodejs/puzzlescript/engine.js b/puzzlescript_nodejs/puzzlescript/engine.js
index db0db52..fb81822 100644
--- a/puzzlescript_nodejs/puzzlescript/engine.js
+++ b/puzzlescript_nodejs/puzzlescript/engine.js
@@ -125,6 +125,7 @@ function createSandbox() {
 
     sandbox.canvasResize = function() {};
     sandbox.redraw = function() {};
+    sandbox.updateFocusBorderColour = function() {}; // scriptprof: upstream colorhelpers.js not bundled
     sandbox.forceRegenImages = function() {};
     sandbox.consolePrintFromRule = function() {};
     sandbox.consolePrint = function() {};
